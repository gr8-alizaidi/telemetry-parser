# Telemetry Demo — Project Context Map

Generated by Decispher. Do not edit.

## Always-true rules

These are CRITICAL invariants. Every AI agent loading this project should treat them as non-negotiable.

- **Adopt goldens/golden-v2.json as telemetry-parser gold standard to retain null-user events** — Adopt `goldens/golden-v2.json` as the new gold standard for `telemetry-parser` going forward, deprecating `golden-v1.json`. All future changes to `src/parser.js` or `scripts/compare.js` must validate against v2 only, which keeps system…

## Topics (load on demand)

**Local stubs:** `.decispher/topics/<topic>.md` — spine inline, unit ID list.
**MCP tools:** `decispher.list_topics()` · `decispher.get_context_for_topic({ topic })` · `decispher.get_decision({ decisionId })` · `decispher.search_decisions({ query })` · `decispher.check_intent({ description })`

| Topic | Label | Units |
|-------|-------|------:|
| `c/decispher-demo-mri2fy8z/gold-standard` | Gold Standard | 1 |

## Recent changes

- 2026-07-14 · Adopt goldens/golden-v2.json as telemetry-parser gold standard to retain null-user events

## Need something not here?

Call `decispher.list_topics()` for the live topic list (may differ from this file if new topics were added since the last sync).

Call `decispher.search_decisions({ query: "your question" })` to search across all context.
